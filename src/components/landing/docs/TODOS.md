# Landing — Todos

## Pending
- [ ] Create `/about` route — header links to it but route doesn't exist
- [ ] Add responsive mobile menu for nav links
- [ ] Extract nav link config from `header.tsx` into a data file
- [ ] Add keyboard navigation for dropdown

## Completed
- [x] Fix `ThemeToggle` import case in `header.tsx`
- [x] Add proper barrel export in `index.ts`
- [x] Create `hooks/` directory
- [x] Add `docs/` lifecycle documentation
