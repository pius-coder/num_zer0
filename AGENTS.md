# Agent Instructions

## Communication

- **Passé**: what was done (commit ref, files changed)
- **Présent**: what is being worked on now
- **Futur**: what comes next (choices if multiple)

## Coding conventions

- No comments in code
- Concise responses (<4 lines unless asked)
- Edit existing files, don't create new ones unless explicitly required
- Mimic code style, use existing libraries and patterns
- Prefer barrel imports (`@/aura/client`, `@/aura/server`) over deep paths

## Deprecations

- When a new approach replaces an old one, mark the old as `~~DEPRECATED~~` in TRACKER.md decision table with the new motivation
- Never delete a decision — strike-through and explain why

## Workflow

1. Read TRACKER.md on session start
2. Read relevant source files before editing
3. Run type check after changes
4. Update CHANGELOG.md and TRACKER.md before committing
5. Commit message format: `theme N: description (details)`
6. Push after each commit

## Build

- `bun x tsc --noEmit --project packages/aura/tsconfig.json` for framework
- `bun x tsc --noEmit --project apps/app/tsconfig.json` for app

---

<context>
  <project name="Aura" runtime="bun" monorepo="true">
    <structure>
      <package name="@aura-js/core" path="packages/aura/src/">
        <dir name="server">Hono runtime (routes, middleware, operations, registry, runner, dashboard, observability)</dir>
        <dir name="client">React hooks (useQuery, useMutation, AuraProvider, stepper, form)</dir>
        <dir name="ui">shadcn UI components (71 components, import via #/aura/ui/)</dir>
        <dir name="cli">CLI codegen, make stubs, doctor</dir>
        <dir name="core">Shared types, errors, envelopes</dir>
        <dir name="shared">Manifest types, schemas</dir>
      </package>
      <package name="@aura-js/app" path="apps/app/src/">
        <dir name="routes">TanStack Start file-based routes (routeTree.gen.ts generated by Vite)</dir>
        <dir name="operations">Aura operations (system/, todos/, ai/, catalog/)</dir>
        <file name="aura.registry.ts">Registry import side-effect — imports all operation modules</file>
      </package>
    </structure>

    <decisions>
      <decision id="D1" status="DEPRECATED">
        <old>Monorepo pnpm workspaces</old>
        <new>Bun workspaces</new>
        <reason>bun est le runtime unique, pas besoin de pnpm</reason>
      </decision>
      <decision id="D2" status="DEPRECATED">
        <old>Routes code-based (pas file-based)</old>
        <new>File-based avec routeTree.gen.ts</new>
        <reason>TanStack Start nécessite la génération du route tree pour le SSR bundling</reason>
      </decision>
      <decision id="D3" status="ACTIVE">Aura est un workspace package (pas npm publish)</decision>
      <decision id="D4" status="ACTIVE">Hono est le socle HTTP — portable, testable, séparable</decision>
      <decision id="D5" status="ACTIVE">Dashboard = SPA servie par Hono — indépendant du frontend principal</decision>
      <decision id="D6" status="ACTIVE">EventBus in-memory (pas de DB) — assez pour MVP</decision>
      <decision id="D7" status="DEPRECATED">
        <old>Dashboard frontend React mini-app build séparé</old>
        <new>Vanilla JS + Chart.js CDN, zéro build step, servie directement par Hono</new>
        <reason>MVP, zéro build step, React si le dashboard devient complexe</reason>
      </decision>
      <decision id="D8" status="ACTIVE">Philosophie : réécrire, pas adapter — supprimer les aliases legacy et les shims</decision>
    </decisions>

    <api layer="client" canonical="true">
      <!-- These are the ONLY exported names from @/aura/client — old names removed -->
      <export name="AuraProvider" from="provider" />
      <export name="callAura" from="transport" />
      <export name="configureAura" from="transport" />
      <export name="fetchManifest" from="transport" />
      <export name="useMutation" from="hooks" />
      <export name="useQuery" from="hooks" />
      <export name="useStepperForm" from="stepper" />
      <export name="usePaginatedQuery" from="paginated-query" />
      <export name="useAgentThread" from="agent" />
      <export name="useAgentStream" from="agent" />
      <export name="useAgentSend" from="agent" />
    </api>

    <current_state last_commit="1208606">
      <theme id="1" name="Split Déploiement" status="done">
        <item>server-hono.ts entrypoint Hono standalone</item>
        <item>build:backend (bun build), build:frontend (vite build)</item>
        <item>Dockerfiles backend + frontend</item>
        <item>RouteRules Nitro pour proxy en prod</item>
      </theme>
      <theme id="2" name="Dashboard" status="done">
        <phase number="1">
          <item>event-bus.ts — EventBus in-memory (subscribers, ring buffer 1000 events)</item>
          <item>metrics.ts — MetricsStore per-function (p50/p90/p99, fenêtre 1m)</item>
          <item>runner.ts — console.log capture + event emission on every execution</item>
          <item>dashboard/routes.ts — 6 REST endpoints (/api/functions, /api/logs, /api/errors, /api/metrics, /api/functions/:name, /api/functions/:name/run, /api/functions/:name/history)</item>
          <item>SPA vanilla JS (Logs, Functions, Errors, Metrics with auto-refresh)</item>
        </phase>
        <phase number="2">
          <item>Chart.js pour graphiques metrics (error frequency bar chart, 5-min buckets)</item>
          <item>Vue request ID (click → modal avec tous les events d'une même requête)</item>
          <item>Run function modal avec éditeur JSON + résultat formaté</item>
          <item>Détail par fonction (grid latence, logs récents, run history)</item>
          <item>Error trending (total, affected functions, top error, bar chart)</item>
        </phase>
      </theme>
      <theme id="3" name="Hono Middleware" status="partial">
        <item status="done">CORS global via hono/cors (origin: AURA_APP_URL)</item>
        <item status="done">Logger middleware — [aura] METHOD /path STATUS DURATIONms</item>
        <item status="done">CSRF middleware (scoped to bridge router)</item>
        <item status="done">Rate-limit middleware (in-memory, per-key)</item>
        <item status="todo">Auth middleware (internal secret, api key)</item>
      </theme>
      <theme id="4" name="Standardisation DX" status="done">
        <item>Phase 1: Builders chainable (defineAgent, defineDbReadFn, defineSearchIndex, defineVectorIndex)</item>
        <item>Phase 2: Client hooks — useQuery/useMutation callables, args plats</item>
        <item>Phase 3: ref/ supprimé, audit rewrite (barrel exports, stubs, types)</item>
      </theme>
    </current_state>

    <pitfalls>
      <pitfall description="packages/aura/ importe @/generated/prisma/client (app-specific)">
        <workaround>tsconfig paths pointent vers ../../apps/app/src/generated/prisma/</workaround>
      </pitfall>
      <pitfall description="UI files import #/aura/ui/* (Vite alias) et @/lib/utils, @/components/*, @/hooks/* (app-specific)">
        <workaround>Ces imports ne marchent que dans le contexte apps/app/ (Vite configuré)</workaround>
      </pitfall>
      <pitfall description="context-adapter.ts importe @tanstack/start-server-core">
        <workaround>Ne pas inclure dans le barrel serveur si backend standalone</workaround>
      </pitfall>
      <pitfall description="call.ts et manifest-injector.tsx importent @/aura.registry (side-effect)">
        <workaround>Module introuvable dans packages/aura/ — ne compile que dans apps/app/</workaround>
      </pitfall>
      <pitfall description="packages/aura/package.json a TOUTES les dépendances (30+)">
        <workaround>Ne pas hésiter à en ajouter — Vite 8/Rolldown échoue si manquantes</workaround>
      </pitfall>
      <pitfall description="Pre-existing TS errors (call.ts, manifest-injector, context-adapter, provider.tsx, agent.ts)">
        <workaround>Ignorer — hors scope des corrections, liés à l'import d'app-specific ou de types manquants</workaround>
      </pitfall>
    </pitfalls>

    <audit_results date="2026-05-24" tool="madge+manual">
      <circular_deps>
        <dep files="server/context.ts ↔ server/notifications.ts" severity="low" status="false_positive">
          <note>import type only — les deux modules n'échangent que des types. Effacé à la compilation.</note>
        </dep>
        <dep files="server/operation.ts ↔ server/registry.ts" severity="low" status="false_positive">
          <note>operation.ts importe registerOperation (valeur) ; registry.ts importe type RegisteredAuraOperation (effacé). Pas de cycle runtime.</note>
        </dep>
        <dep files="server/ai/agent.ts → create-context.ts → context-binding.ts → agent.ts" severity="low" status="false_positive">
          <note>agent.ts utilise await import() dynamique. Pas de cycle module-level. madge le détecte via l'import dynamique.</note>
        </dep>
      </circular_deps>
      <any_types>
        <finding file="server/routes/bridge.ts:59" status="fixed">
          <code>Context&lt;any, any, any&gt;</code>
          <note>Remplacé par Context (sans generics). Hono accepte Context par défaut.</note>
        </finding>
        <finding file="client/stepper.ts:75" status="kept">
          <code>as any</code>
          <note>zodResolver générique complexe — cast nécessaire. eslint-disable no-explicit-any présent.</note>
        </finding>
        <note>0 violations restantes (bridge.ts fixé, stepper.ts est un cas légitime). Tous les autres as unknown as sont des casts nécessaires pour builder pattern, dynamic dispatch, Prisma proxying.</note>
      </any_types>
      <patch_wrappers>
        <finding status="fixed">client/index.ts — useAuraForm et useAuraParams conservés (pas d'alias court possible — useForm conflit avec react-hook-form, useParams avec React Router). Tout le reste est nettoyé.</finding>
        <finding status="kept">provider.tsx, transport.ts, hooks.ts, paginated-query.ts, agent.ts — alias internes (export const Foo = Bar) conservés. Le barrel n'exporte que les nouveaux noms.</finding>
        <finding status="kept">ui/aura-form.tsx, ui/aura-data-table.tsx — doc comments mis à jour (useMutation, usePaginatedQuery au lieu de useAura*).</finding>
      </patch_wrappers>
      <legacy_branches>
        <finding file="client/stepper.ts" status="fixed">Dead code supprimé (constructor dans interface + ... placeholders). useStepperForm fonctionnel.</finding>
        <finding file="server/routes/files.ts:54" status="kept">Mode 2 legacy free-path serving — gardé car les projets utilisateurs peuvent avoir des fichiers en filesystem.</finding>
        <finding file="server/middleware/rate-limit.ts" status="kept">Commentaires references legacy — documentation uniquement, pas de code actif.</finding>
      </legacy_branches>
      <summary>
        <item>0 any types réels (bridge.ts:59 fixé)</item>
        <item>3 circular deps — toutes false positives (type-only ou dynamic import)</item>
        <item>0 patch wrappers dans le barrel public (useAuraForm/useAuraParams conservés car pas d'alias possible)</item>
        <item>0 dead code actif (stepper.ts fixé, files.ts légitime)</item>
        <item>1 eslint-disable no-explicit-any (stepper.ts:75 — zodResolver générique)</item>
        <item>0 dépendances circulaires runtime</item>
      </summary>
    </audit_results>

    <next_actions>
      <option priority="high">Auth middleware Hono (internal secret HMAC, api key)</option>
      <option priority="medium">Dashboard Phase 2.5: WebSocket temps réel pour logs (remplacer polling 3s)</option>
      <option priority="low">Data browser dashboard (tables, documents, custom queries)</option>
      <option priority="low">Thème 1: tester le split complet (deux artefacts déployés)</option>
    </next_actions>
  </project>
</context>
